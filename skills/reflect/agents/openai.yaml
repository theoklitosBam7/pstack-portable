interface:
  display_name: "Reflect"
  short_description: "Turn task learnings into improvements to skills"
  default_prompt: "Use $reflect to capture task learnings as concrete skill improvements."

policy:
  allow_implicit_invocation: false
