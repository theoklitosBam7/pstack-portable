interface:
  display_name: "Type-System Discipline"
  short_description: "Use types to encode valid program states"
  default_prompt: "Use $principle-type-system-discipline to make this typed code state-safe."

policy:
  allow_implicit_invocation: false
