interface:
  display_name: "Make Operations Idempotent"
  short_description: "Make retries converge to one end state"
  default_prompt: "Use $principle-make-operations-idempotent to make this operation safe to retry."

policy:
  allow_implicit_invocation: false
